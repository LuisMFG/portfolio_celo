// next-intl.config.ts
import type { IntlConfig } from 'next-intl';

const config: IntlConfig = {
  // seus idiomas
  locale: ['pt', 'en'],

  // idioma padrão
  defaultLocale: 'pt'
};

export default config;
